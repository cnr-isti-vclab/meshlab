After a full rebuild of meshlab_full.pro run the macinstall_v2016.sh script.
It should works either from a build inside qtcreator and shadow build 
or from a standard qmake/make 
 
The script will generate a relocable app folder and it will build a dmg, 
